**方案已确认，请按以下修正点执行：**

你的实现方案整体很扎实。针对“用户审查 (User Review)”部分，请确认并修正以下关键细节：

1. **数据库字段（修正）**：

- 表名确实是 `user_emo_audio`。
- **修正**：存储克隆声音文件路径的字段名明确是 **`output_path`**（不是 `spk_audio_prompt`）。请在代码中获取该字段的值作为 pipeline 的 `input_wav` 参数。

2. **配置文件路径（确认）**：

- 确认路径模式严格为：`tts-story/config/story_library_{story_id}.json`。

3. **配置文件内容（假设）**：

- 我们假设该 JSON 配置文件中已包含以下键值：`json_db`, `emo_audio_folder`, `bgm_path`, 以及 `script_json`。

4. **错误处理**：

- 如果配置文件不存在，或者用户在数据库中没有音频记录，请返回清晰的 JSON 错误信息（例如：“未找到故事配置”或“请先生成您的克隆声音”），以便前端展示给用户。

**执行行动 (Action):**

请开始编写代码：

1. 更新 **FastAPI 后端**（创建 `app/services/business_generate.py` 并更新 `app/main.py`）。
2. 创建前端 API 文件 **`src/api/audioTask.js`**。
3. 重构 **`StoryLibraryPage.vue`**，实现基于 LocalStorage 的状态持久化和轮询逻辑。

**开始写代码吧。**
